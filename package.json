{
	"name": "astro",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro preview",
		"astro": "astro",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name blog --branch main",
		"deploy:preview": "npm run build && wrangler pages deploy dist --project-name blog --branch master"
	},
	"dependencies": {
		"@astrojs/mdx": "^4.3.13",
		"@tailwindcss/vite": "^4.2.1",
		"astro": "^5.18.0",
		"tailwindcss": "^4.2.1"
	},
	"devDependencies": {
		"prettier": "^3.8.1",
		"prettier-plugin-astro": "^0.14.1"
	}
}
